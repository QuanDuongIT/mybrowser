services:
  - type: web
    name: flask-playwright
    env: python

    envVars:
      - key: PLAYWRIGHT_BROWSERS_PATH
        value: 0

    buildCommand: |
      pip install -r requirements.txt
      playwright install chromium

    startCommand: gunicorn app:app --timeout 120